BACKEND_BASE_URL=http://localhost:8000
# Exposed to the browser (resume upload calls FastAPI directly).
# Inlined at build time - rebuild after changing it.
NEXT_PUBLIC_BACKEND_BASE_URL=http://localhost:8000
